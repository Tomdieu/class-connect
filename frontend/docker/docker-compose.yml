version: '3.8'

services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "3001:3001"
    restart: unless-stopped
    env_file:
      - ../.env  # Make sure the path is correct
